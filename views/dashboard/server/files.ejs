<%- include('../../components/head') %>

<body class="min-h-screen bg-neutral-950 text-slate-100">
  <%- include('../../components/modal') %>
  <div>
    <%- include('../../components/navigation') %>
    <div class="md:pl-64 flex flex-col flex-1 min-h-screen">
      <%- include('../../components/header') %>
      <main class="flex-1 pb-8">
        <div class="py-4 md:py-6">
          <%- include('../../components/skeleton') %>
          <div class="hidden max-w-screen-2xl mx-auto px-4 sm:px-6 md:px-8" id="content">
            <%- include('./header') %>
            <%- include('./tabs') %>

            <div class="mt-6 grid grid-cols-1 lg:grid-cols-2 gap-6">
              <div class="bg-neutral-900/60 border border-neutral-800/80 rounded-2xl p-6 space-y-4">
                <div>
                  <p class="text-xs uppercase tracking-[0.3em] text-slate-500">File manager</p>
                  <h2 class="text-xl font-semibold text-white">Browse files</h2>
                </div>
                <p class="text-sm text-slate-400">
                  Inspect the server root, download configs, and keep backups in sync. These shortcuts point toward the files view inside the panel.
                </p>
                <dl class="grid grid-cols-2 gap-3 text-sm text-slate-400">
                  <div>
                    <dt class="text-[0.65rem] uppercase tracking-[0.3em] text-slate-500">Root path</dt>
                    <dd class="text-white font-semibold mt-1"><%= server.attributes.identifier ? `/srv/daemon-data/${server.attributes.identifier}` : "/srv/daemon-data" %></dd>
                  </div>
                  <div>
                    <dt class="text-[0.65rem] uppercase tracking-[0.3em] text-slate-500">Startup file</dt>
                    <dd class="text-white font-semibold mt-1"><%= server.attributes.build && server.attributes.build.startup ? server.attributes.build.startup : "Auto" %></dd>
                  </div>
                </dl>
                <div class="space-y-3">
                  <div class="rounded-2xl bg-neutral-950/70 border border-neutral-800/70 p-4">
                    <p class="text-xs uppercase tracking-[0.3em] text-slate-500 mb-2">Common directories</p>
                    <div class="flex flex-wrap gap-2 text-xs">
                      <span class="px-3 py-1 rounded-full bg-neutral-900 border border-neutral-800">/logs</span>
                      <span class="px-3 py-1 rounded-full bg-neutral-900 border border-neutral-800">/config</span>
                      <span class="px-3 py-1 rounded-full bg-neutral-900 border border-neutral-800">/plugins</span>
                      <span class="px-3 py-1 rounded-full bg-neutral-900 border border-neutral-800">/data</span>
                    </div>
                  </div>
                  <div class="rounded-2xl bg-neutral-950/70 border border-neutral-800/70 p-4">
                    <p class="text-sm text-slate-300">
                      The dashboard surface is read-only for files. Use the official panel to stream uploads, download backups, and edit configuration files inside the binder.
                    </p>
                  </div>
                </div>
                <a
                  href="<%= serverConsoleUrl %>/files"
                  target="_blank"
                  rel="noreferrer"
                  class="inline-flex items-center justify-center rounded-2xl bg-sky-500/90 px-4 py-2 text-sm font-semibold text-white hover:bg-sky-400 transition"
                >
                  Open panel file manager
                </a>
              </div>

              <div class="bg-neutral-900/60 border border-neutral-800/80 rounded-2xl p-6 space-y-4">
                <div>
                  <p class="text-xs uppercase tracking-[0.3em] text-slate-500">Backup</p>
                  <h2 class="text-xl font-semibold text-white">Snapshot readiness</h2>
                </div>
                <p class="text-sm text-slate-400">
                  Use the panel's file manager to export compressed backups or sync files with SFTP. Whoever laces your directories is visible through the official console.
                </p>
                <div class="rounded-2xl bg-neutral-950/70 border border-neutral-800/70 p-4 space-y-2 text-sm text-slate-400">
                  <p>
                    Backups Active:
                    <span class="text-white font-semibold"><%= server.attributes.feature_limits && server.attributes.feature_limits.backups ? server.attributes.feature_limits.backups : "Unlimited" %></span>
                  </p>
                  <p>
                    Backup Locations:
                    <span class="text-white font-semibold">Panel storage</span>
                  </p>
                </div>
                <p class="text-xs text-slate-500">
                  Scheduled downloads live inside the panel and are not proxied through this dashboard.
                </p>
              </div>
            </div>
          </div>
        </div>
      </main>
      <%- include('../../components/footer') %>
    </div>
  </div>
  <%- include('../../components/notifications') %>
</body>
